# Byte-compiled / cache
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtualenv
.venv/
venv/

# Env / secrets
.env
.env.local
.env.*.local
!.env.example

# OS / IDE
.DS_Store
.idea/
.vscode/

# Local artifacts (regenerated by bootstrap; keep metrics schema in repo via CI)
# We commit seeded demo artifacts so the Streamlit app runs without a train step.
# Uncomment to ignore if regenerating locally:
# data/raw/
# data/processed/
# models/*.joblib
# models/rag_index/
